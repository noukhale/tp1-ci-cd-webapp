{
  "name": "tp1-ci-cd-webapp",
  "version": "1.0.0",
  "description": "Webapp Node.js simple sans tests",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "echo \"No tests configured\" && exit 0",
    "build": "echo \"Nothing to build for Node.js app\""
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
